//! GPU resource isolation and quota enforcement for AI agent sandboxes.
//!
//! Stub implementation — full GPU isolation will be added in a subsequent milestone.

/// GPU isolation configuration (stub)
#[derive(Debug, Clone, Default)]
pub struct GpuIsolation {
    pub enabled: bool,
}
